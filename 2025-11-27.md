- 14:05 `DESCRIBE question_answers` を実行し、`question_mode` と旧 `answer_mode` が同居していたため INSERT 失敗の原因が DB スキーマ未更新であることを確認。
- 14:10 `ALTER TABLE question_answers DROP COLUMN answer_mode;` を root 権限で実行し、旧カラムを削除。
- 14:11 再度 `DESCRIBE question_answers;` を実施して `question_mode` のみが残っていることを確認。CBT 回答 API が再び挿入できる状態になった旨を共有。
