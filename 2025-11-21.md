- 09:30 `/srv/rag-demo/app` のこれまでの内容を `/srv/rag-demo/app_pre_20251121` へ退避し、新リポジトリを導入できる状態に整理。
- 09:35 `https://github.com/Igaki12/News-network-UI-demo.git` を `/srv/rag-demo/app` にクローンし、リポジトリ同梱の AGENTS.md を `AGENTS_for_React.md` へ改名。`package.json` のビルド手順（`dist` 生成後に `docs/` へコピー）を確認し、VPS 配信用の dist と GitHub Pages 用 docs を切り分ける運用を再確認。
- 10:10 DB ガイド (`AGENTS_about_DB.md`) の question_answers から `answer_mode` カラムを削除し、React 側の 3 種類の出題モードを記録する `question_mode` カラムだけで管理する方針に更新。合わせて `/srv/rag-demo/db/schema_news_learning.sql` へ同変更を反映。
- 10:45 `/srv/rag-demo/app` で `npm install`→`npm run build` を実行し、生成した `dist/` を `/var/www/rag-demo/` へ rsync。Apache を reload し、`systemctl is-active apache2` で稼働を確認（ローカル curl は環境制限で不可）。
- 11:05 Vite 設定で `VITE_BASE_PATH`（未指定時は `/`）を参照するよう変更し、`npm run build`→`rsync dist -> /var/www/rag-demo`→Apache reload を再実施。これにより VPS ではルート配信、GitHub Pages 用ビルドでは環境変数でベースパスを切り替え可能になった。
